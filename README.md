"# Sprint_6" 
# Проект автоматизации тестирования сервиса «Яндекс.Самокат»
1. Основа для написания автотестов — фреймворк pytest.
2. Установить зависимости:
— pip3 install -r requirements.txt
— pip install -r requirements.txt (если Python второй версии)

По умолчанию тесты выполняются без отрисовки интерфейса,
чтобы видеть выполнение тестов в браузере, в файле file confest.py закомментируйте эту строку кода:
"# options.add_argument("--headless")"